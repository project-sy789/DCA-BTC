# 🚀 การติดตั้งแบบด่วน (Quick Install)

## ขั้นตอนการติดตั้ง 3 ขั้นตอน

### 1️⃣ Build โปรเจค

```bash
cd dca-bitcoin-tracker
npm install
npm run build
```

### 2️⃣ Upload ไฟล์

Upload ไฟล์เหล่านี้ไปยัง hosting:

- **ไฟล์ใน `dist/`** → ไปที่ `public_html/`
- **โฟลเดอร์ `api/`** → ไปที่ `public_html/api/`

### 3️⃣ รันตัวติดตั้ง

เปิดเว็บเบราว์เซอร์:

```
https://yourdomain.com/api/install.php
```

กรอกข้อมูล database และคลิก "ถัดไป" → เสร็จแล้ว! 🎉

---

## ข้อมูล Database ที่ต้องเตรียม

จาก hosting control panel (cPanel/Plesk) ของคุณ:

- ✅ Database Host (มักเป็น `localhost`)
- ✅ Database Username
- ✅ Database Password
- ✅ Database Name (สร้างใหม่หรือใช้ที่มีอยู่)

---

## หลังติดตั้งเสร็จ

ลบไฟล์ติดตั้งเพื่อความปลอดภัย:

```bash
# ลบไฟล์เหล่านี้จาก hosting
api/install.php
api/config.template.php
```

---

## ต้องการความช่วยเหลือ?

อ่านคู่มือฉบับเต็มที่ [INSTALLATION.md](INSTALLATION.md)
